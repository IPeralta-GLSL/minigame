[package]
name = "app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
engine = { path = "../engine" }
tinycraft = { path = "../tinycraft" }
solar_system = { path = "../solar_system" }
crossy_road = { path = "../crossy_road" }
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
serde = { version = "1.0", features = ["derive"] }
serde-wasm-bindgen = "0.6"
web-sys = { version = "0.3", features = [
    "Window",
    "Document",
    "Element",
    "HtmlCanvasElement",
    "WebGlRenderingContext",
    "KeyboardEvent",
    "MouseEvent",
    "WheelEvent",
    "Request",
    "RequestInit",
    "RequestMode",
    "Response",
    "Headers",
] }
